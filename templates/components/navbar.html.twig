<nav class="navbar navbar-expand-lg navbar-dark bg-dark py-4">
	<div class="container">
		<a class="navbar-brand" href="{{ path('home.index') }}">OurNetwork</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			{% if app.user %}
			<form class="d-flex me-auto mb-2 mb-lg-0 ms-3" method="GET" action="{{ path('search') }}">
				<input class="form-control me-2" type="search" name="q" placeholder="Search users" value="{{ app.request.query.get('q') ?? "" }}" aria-label="Search">
				<button class="btn btn-outline-success" type="submit">Search</button>
			</form>
			{% else %}
			<div class="me-auto mb-2 mb-lg-0"></div>
			{% endif %}
			<ul class="navbar-nav ">
				{% if is_granted('ROLE_MODERATOR') %}
					<li class="nav-item">
						<a class="nav-link" aria-current="page" href="{{ path('dashboard') }}">
							<i class="bi bi-back"></i>
						</a>
					</li>
				{% endif %}
				{% if app.user %}
					<li class="nav-item ms-4">
						<a class="nav-link" aria-current="page" href="{{ path('auth.logout') }}">
							<i class="bi bi-box-arrow-left"></i>
						</a>
					</li>
				{% else %}
					<li class="nav-item ms-4">
						<a class="nav-link" aria-current="page" href="{{ path('auth.login') }}">
							<i class="bi bi-box-arrow-in-right"></i>
						</a>
					</li>
					<li class="nav-item ms-4">
						<a class="nav-link" aria-current="page" href="{{ path('auth.register') }}">
							<i class="bi bi-person-plus-fill"></i>
						</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
